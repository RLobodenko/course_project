{% extends 'base.html.twig' %}

{% block title %}Create new item collection{% endblock %}

{% block body %}
<P class="m-3">
<h2>Edit item {{item.name}}</h2>
{{ form_start(form, {'attr': {'class': 'custom-form-class'}}) }}
    {{ form_row(form.name, {'attr' : {'class': 'form-control'}}) }}
    {{ form_row(form.tags, {'attr' : {'class': 'form-control'}}) }}
    <button>Update</button>
{{ form_end(form) }}
<a href='/collectionns/'>Back list collections</a></P>
{% endblock %}
